RewriteEngine On
RewriteBase /

# Serve existing files and folders first.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback for TanStack Router / SPA routes.
RewriteRule ^ index.html [L]
